spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: validate

  datasource:
    platform: postgres
    url: jdbc:postgresql://192.168.2.217:5432/tyh
    username: postgre
    password: HFcb2020
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 1
  main:
    allow-bean-definition-overriding: true

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults: false